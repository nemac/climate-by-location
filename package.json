{
  "name": "climate-widget-graph",
  "version": "0.0.0",
  "description": "Climate Widget is a jQuery UI Widget that can be declared on a div that in turn creates a Climate Widget data graph.",
  "main": "gulpfile.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "gulp": "^3.9.0",
    "gulp-rename": "^1.2.2",
    "gulp-uglify": "^1.4.2"
  }
}
